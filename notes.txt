Aufbau:

	-class Compilier
		-constructor method (GUI)
			=2xCntrlBox
			=1xDropdown to choose the output format, Radiobox
			=1x'enter' button( calls method compiler)
		
		-method compile(stringtocompile(cntrlbox), output_format(dropdown))
		
		-menubar
			-file
				save to .txt
				---------
				exit
			
			-
				
	
extras__:
	-menubar
		-ability to change the font colour in all of the text fields
		-ability to change the font size in all text fields
	
	-when dropdown select is morse-code
		--> a second button appears beside the main compile button, which plays the morse code as dots and dashes			
		

	